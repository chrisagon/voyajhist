<?php
// This script and data application were generated by AppGini 5.61
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir=dirname(__FILE__);
	include("$currDir/defaultLang.php");
	include("$currDir/language.php");
	include("$currDir/lib.php");
	@include("$currDir/hooks/connect_perso_seq.php");
	include("$currDir/connect_perso_seq_dml.php");

	// mm: can the current member access this page?
	$perm=getTablePermissions('connect_perso_seq');
	if(!$perm[0]){
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout("window.location=\'index.php?signOut=1\'", 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = "connect_perso_seq";

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = array(   
		"`connect_perso_seq`.`id`" => "id",
		"IF(    CHAR_LENGTH(`personnages1`.`nom`) || CHAR_LENGTH(`personnages1`.`type`), CONCAT_WS('',   `personnages1`.`nom`, ' ', `personnages1`.`type`), '') /* Perso */" => "perso",
		"IF(    CHAR_LENGTH(`sequence1`.`titre`), CONCAT_WS('',   `sequence1`.`titre`), '') /* Seq */" => "seq"
	);
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = array(   
		1 => '`connect_perso_seq`.`id`',
		2 => 2,
		3 => '`sequence1`.`titre`'
	);

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = array(   
		"`connect_perso_seq`.`id`" => "id",
		"IF(    CHAR_LENGTH(`personnages1`.`nom`) || CHAR_LENGTH(`personnages1`.`type`), CONCAT_WS('',   `personnages1`.`nom`, ' ', `personnages1`.`type`), '') /* Perso */" => "perso",
		"IF(    CHAR_LENGTH(`sequence1`.`titre`), CONCAT_WS('',   `sequence1`.`titre`), '') /* Seq */" => "seq"
	);
	// Fields that can be filtered
	$x->QueryFieldsFilters = array(   
		"`connect_perso_seq`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`personnages1`.`nom`) || CHAR_LENGTH(`personnages1`.`type`), CONCAT_WS('',   `personnages1`.`nom`, ' ', `personnages1`.`type`), '') /* Perso */" => "Perso",
		"IF(    CHAR_LENGTH(`sequence1`.`titre`), CONCAT_WS('',   `sequence1`.`titre`), '') /* Seq */" => "Seq"
	);

	// Fields that can be quick searched
	$x->QueryFieldsQS = array(   
		"`connect_perso_seq`.`id`" => "id",
		"IF(    CHAR_LENGTH(`personnages1`.`nom`) || CHAR_LENGTH(`personnages1`.`type`), CONCAT_WS('',   `personnages1`.`nom`, ' ', `personnages1`.`type`), '') /* Perso */" => "perso",
		"IF(    CHAR_LENGTH(`sequence1`.`titre`), CONCAT_WS('',   `sequence1`.`titre`), '') /* Seq */" => "seq"
	);

	// Lookup fields that can be used as filterers
	$x->filterers = array(  'perso' => 'Perso', 'seq' => 'Seq');

	$x->QueryFrom = "`connect_perso_seq` LEFT JOIN `personnages` as personnages1 ON `personnages1`.`id_perso`=`connect_perso_seq`.`perso` LEFT JOIN `sequence` as sequence1 ON `sequence1`.`id_sequence`=`connect_perso_seq`.`seq` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm[2]==0 ? 1 : 0);
	$x->AllowDelete = $perm[4];
	$x->AllowMassDelete = false;
	$x->AllowInsert = $perm[1];
	$x->AllowUpdate = $perm[3];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 0;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation["quick search"];
	$x->ScriptFileName = "connect_perso_seq_view.php";
	$x->RedirectAfterInsert = "connect_perso_seq_view.php?SelectedID=#ID#";
	$x->TableTitle = "Connect perso seq";
	$x->TableIcon = "table.gif";
	$x->PrimaryKey = "`connect_perso_seq`.`id`";

	$x->ColWidth   = array(  150, 150);
	$x->ColCaption = array("Perso", "Seq");
	$x->ColFieldName = array('perso', 'seq');
	$x->ColNumber  = array(2, 3);

	// template paths below are based on the app main directory
	$x->Template = 'templates/connect_perso_seq_templateTV.html';
	$x->SelectedTemplate = 'templates/connect_perso_seq_templateTVS.html';
	$x->TemplateDV = 'templates/connect_perso_seq_templateDV.html';
	$x->TemplateDVP = 'templates/connect_perso_seq_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->ShowRecordSlots = 0;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HighlightColor = '#FFF0C2';

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, array('user', 'group'))){ $DisplayRecords = 'all'; }
	if($perm[2]==1 || ($perm[2]>1 && $DisplayRecords=='user' && !$_REQUEST['NoFilter_x'])){ // view owner only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `connect_perso_seq`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='connect_perso_seq' and lcase(membership_userrecords.memberID)='".getLoggedMemberID()."'";
	}elseif($perm[2]==2 || ($perm[2]>2 && $DisplayRecords=='group' && !$_REQUEST['NoFilter_x'])){ // view group only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `connect_perso_seq`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='connect_perso_seq' and membership_userrecords.groupID='".getLoggedGroupID()."'";
	}elseif($perm[2]==3){ // view all
		// no further action
	}elseif($perm[2]==0){ // view none
		$x->QueryFields = array("Not enough permissions" => "NEP");
		$x->QueryFrom = '`connect_perso_seq`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: connect_perso_seq_init
	$render=TRUE;
	if(function_exists('connect_perso_seq_init')){
		$args=array();
		$render=connect_perso_seq_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: connect_perso_seq_header
	$headerCode='';
	if(function_exists('connect_perso_seq_header')){
		$args=array();
		$headerCode=connect_perso_seq_header($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$headerCode){
		include_once("$currDir/header.php"); 
	}else{
		ob_start(); include_once("$currDir/header.php"); $dHeader=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%HEADER%%>', $dHeader, $headerCode);
	}

	echo $x->HTML;
	// hook: connect_perso_seq_footer
	$footerCode='';
	if(function_exists('connect_perso_seq_footer')){
		$args=array();
		$footerCode=connect_perso_seq_footer($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$footerCode){
		include_once("$currDir/footer.php"); 
	}else{
		ob_start(); include_once("$currDir/footer.php"); $dFooter=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%FOOTER%%>', $dFooter, $footerCode);
	}
?>